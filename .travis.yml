##############################################################################
### Autogenerated with R package kwb.pkgbuild v0.1.3
### (installed from 'Github (kwb-r/kwb.pkgbuild@8a0b2e2)' source code on 2020-05-28)
### by calling the function kwb.pkgbuild::use_autopkgdown("kwb.nextcloud")
### (file created at: 2020-05-28 23:26:51)
##############################################################################


language: r
pandoc_version: 2.7.3
sudo: required
cache: packages
r_packages:
- remotes
- covr
matrix:
  include:
  - r: devel
  - r: release
    after_success:
    - Rscript -e 'covr::codecov()'
    before_deploy:
    - Rscript -e 'remotes::install_cran("pkgdown")'
    deploy:
      provider: script
      script: Rscript -e 'pkgdown::deploy_site_github(verbose = TRUE)'
      skip_cleanup: 'true'
      on:
        branch:
        - master
        - dev
  - r: oldrel
